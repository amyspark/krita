[build-system]
requires = ["sip >=5, <7", "PyQt5"]
build-backend = "sipbuild.api"

[tool.sip.metadata]
name = "kritamod"

[tool.sip.project]
sip-include-dirs = ["@PYQT5_SIP_DIR@"]
sip-files-dir = "@PYKRITA_SRCS@"
abi-version = "12"
